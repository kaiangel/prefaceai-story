# Daily Sync — 2026-04-08

---

## 今日进展

### PM — TASK-REAL-PIPELINE-UX 方案设计 + Step 1 派发

**目标**: 已登录用户 StageA→B→C→D 全程真实数据体验（Stage 1-4 真实 LLM + Stage 5 用 R8 已有图片跳过生图）

**方案 2 步**:
- Step 1 @Backend: Stage 5 跳过模式 + generate-outline 返回场景数据 + 生成结果 API
- Step 2 @Frontend: StageC 轮询真实进度 + 角色/场景用真实 outline 数据 + StageD 加载真实结果

**关键发现**: job 状态轮询端点已存在 `GET /api/projects/{id}/chapters/1/status`，不需要新建

**R8 测试数据可复用**: 4 角色参考图 + 场景参考图 + 10 shot 图，Stage 5 跳过时复制这些图片

### Backend — TASK-REAL-PIPELINE-UX Step 1 完成

- 1-A: Stage 5 跳过模式 — `SKIP_IMAGE_GENERATION=true` 时从 R8 复制图片 + pipeline 各阶段 progress_callback
- 1-B: generate-outline 返回 `scenes` 字段（从 `unique_locations` 映射）
- 1-C: `GET /generation-result` + `GET /images/{filename}` 2 个新端点
- 改动: config.py + pipeline_orchestrator.py + job_manager.py + projects.py + .env
- 4 文件 syntax ✅

---

## 等待中

| 事项 | 等谁 | 阻塞 |
|------|------|------|
| ~~REAL-PIPELINE-UX Step 1~~ | ~~@Backend~~ | ✅ 完成 |
| **Step 1 Tester 验证** | **@Tester** | **✅ 35/35 PASS** |
| ~~PM Review Step 1~~ | ~~@PM~~ | ✅ PASS |
| **Step 2 (4 项)** | **@Frontend** | 🔄 已派发 |
| ~~Ben DB 脏数据清理~~ | ~~@Ben~~ | ✅ 完成 (微信确认 04-07 16:00) |

### Tester — REAL-PIPELINE-UX Step 1 验证 (35/35 PASS)

- /xhtdd 完整模式，pytest，零 API 成本
- 1-A Stage 5 跳过模式: 14/14 ✅ (config + R8数据 + 复制/循环/截断)
- 1-B scenes 数据: 6/6 ✅ (映射 + fallback)
- 1-C 生成结果 API: 8/8 ✅ (端点 + 安全 + 结构)
- job_manager 链路: 6/6 ✅
- .env: 1/1 ✅
- 测试脚本: `tests/test_real_pipeline_ux_step1.py`

### PM — Step 1 Review PASS + Step 2 派发

- Backend: 4 文件改动精准，Stage 5 跳过完全隔离，图片端点有安全防护 ✅
- Tester: 35/35 PM 独立跑一致 ✅
- Step 2 派发 @Frontend: 2-A 轮询真实 API + 2-B 角色真实数据 + 2-C 场景真实数据 + 2-D StageD 真实结果

### Frontend — Step 2 完成 (4/4)

- StageC.tsx 重写: text-gen + shot-gen 两阶段均改为 `GET /api/projects/{id}/chapters/1/status` 每 2s 轮询
- 角色预览用 `state.outline.characters`，场景用 `state.outline.scenes`
- `types/create.ts` 新增 `OutlineScene` + `StoryOutline.scenes`
- generation-result 响应映射到 Shot 类型
- 降级: `useRealApi = !!(isLoggedIn && token && projectId)`
- build 20 路由 0 错误
