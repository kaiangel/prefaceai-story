# FastAPI and server
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database
# sqlalchemy 2.0.50: 修 #13306 pymysql do_ping/async-adapter ping 签名 bug
#   (PyMySQL 1.2.0 起 ping() 默认 reconnect=False → 旧 SA do_ping 走无参 ping()
#    → async 适配器 ping(self, reconnect) 无默认 → TypeError → pool_pre_ping 触发即 500)
sqlalchemy==2.0.50
aiosqlite==0.20.0
# asyncmy 0.2.11: dev/prod parity 统一 async 驱动 + 带上游 _auth_plugin_name(#86) bugfix
#   (本次 P0 真修在 sqlalchemy 2.0.50, 非 asyncmy 版本)
asyncmy==0.2.11
greenlet>=3.0.0

# Google Gemini (new official SDK)
google-genai>=1.0.0

# Anthropic Claude (fallback)
anthropic>=0.40.0

# Validation and settings
pydantic==2.10.2
pydantic-settings==2.6.1
email-validator>=2.2.0
python-dotenv==1.0.1

# Utilities
httpx==0.28.1

# ===== Phase 2: Image Generation =====
# Image processing
Pillow>=10.0.0

# Async HTTP (for backup APIs if needed)
aiohttp>=3.9.0

# Database migrations (pymysql used as synchronous driver for alembic CLI)
# pymysql 精确 pin 1.1.2 (2025-08-24, 广泛验证 9 个月) 防止 dev/prod 分裂:
#   原 pymysql>=1.1.0 无上界 → VPS 重建镜像时拉到刚发布(2026-05-19)的 1.2.0
#   → 触发 #13306 ping bug。本地仍是 1.1.2 → "本地测不出 VPS bug" 分裂源。
#   不选 1.2.0(一周前发布, 带 ping reconnect deprecation + TLS 行为变更, 变量太多)。
alembic>=1.13.0
pymysql==1.1.2

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# ===== Phase 3: Audio =====
# OpenAI (Whisper)
openai>=1.0.0
